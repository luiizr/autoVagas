import { Component, inject } from '@angular/core'; import { AsyncPipe } from '@angular/common'; import { ActivatedRoute } from '@angular/router'; import { switchMap } from 'rxjs'; import { JobsService } from '../../services/jobs.service'; @Component({imports:[AsyncPipe],templateUrl:'./job-detail.component.html',styleUrl:'./job-detail.component.scss'}) export class JobDetailComponent {private readonly jobs=inject(JobsService); readonly job=inject(ActivatedRoute).paramMap.pipe(switchMap(params=>this.jobs.get(params.get('id')!)));}
