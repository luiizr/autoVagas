import { inject } from '@angular/core'; import { Router } from '@angular/router'; import { AuthService } from '../services/auth.service'; export const authGuard=()=>inject(AuthService).token()?true:inject(Router).createUrlTree(['/entrar']);
